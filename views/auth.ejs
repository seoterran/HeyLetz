

<% include header %>

<script>
 var obj = {
   name : "<%= name%>",
   already : "<%= already%>"
};
</script>

<div class="container bump-me" >
  <div class="body-content">
      <div class="row">
          <div class="col-lg-6" >
              <div class="panel panel-default">
                  <div class="panel-heading"></div>
                  <div class="panel-body">
                      <%= name%> 님,
                      <% if(already){%>
                          이미 이메일 인증을 마치셨어요!
                      <%}else{%>
                          이메일 인증이 완료 되었습니다. 많은 이용 바랍니다!
                      <%}%>
                  </div>
              </div>
          </div>
      </div>
  </div>
</div>

<% include footer %>
