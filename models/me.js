var userId,
    name,
    email,
    contact,
    gender;


module.exports.create = function(user){
    userId= user._id;
    name=user.name;
    email=user.email;
    contact=user.email;
    gender=user.gender;
}

